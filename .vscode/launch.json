{
    "configurations": [
        {
            "name": "Chrome",
            "type": "pwa-chrome",
            "request": "launch",
            "url": "http://localhost:3000/react_template",
            "webRoot": "${workspaceRoot}/src"
        }
    ]
}